6. React Router, Git, Start the Ecommerce Project

git = helps to track changes in code
.gitignore = tells git which files and folders to ignore (not to track changes)

routing =Create multiple pages in React , Routing lets us create multiple pages using 1 HTML file.
npm install react-router

<Routes> = tells React all the pages that are in our websites 
Routing <Link> Component - prevent reloading/refresh of page while navigating to other pages

When using react-router,  use <Link> instead of <a>
<Link> = go to another page without reloading

SPA = Only 1 html page in Websites

In this lesson:
1. Started the Ecommerce project using React and Vite
2. Git = track changes in our code
3. Routing = create multiple pages in React
4. Created the HomePage, CheckoutPage, OrdersPage
5. Updated all the links to work with routing
6. Separated the Header into a component